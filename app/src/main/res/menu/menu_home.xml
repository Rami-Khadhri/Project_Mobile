<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add_course"
        android:icon="@drawable/ic_add"
        android:title="Add Course"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_search_course"
        android:icon="@drawable/ic_search"
        android:title="Search"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_manage_teachers"
        android:title="Manage Teachers"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_clear_courses"
        android:title="Clear All Courses"
        app:showAsAction="never" />
</menu>
